export interface Chuleton {
    id: number;
    tipo: string;
    origen: string;
    precio: number;
    peso: number;
    cantidad: number;
}
export interface User {
    id: number;
    nombre: string;
    email: string;
    password: string;
}